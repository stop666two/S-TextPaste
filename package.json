{
  "name": "s-textpaste",
  "version": "1.0.0",
  "private": true,
  "description": "Zero-trust end-to-end encrypted text sharing with post-quantum triple-envelope encryption",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "node worker/server.js",
    "build": "cd frontend && npm install && npm run build && node ../scripts/copy-build.js",
    "deploy": "npm run build && wrangler deploy",
    "db:create": "wrangler d1 create s-textpaste-db",
    "postinstall": "cd frontend && npm install"
  },
  "devDependencies": {
    "wrangler": "^3.107.0"
  }
}
