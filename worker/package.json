{
  "name": "s-textpaste-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create s-textpaste-db",
    "db:init": "wrangler d1 execute s-textpaste-db --local --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.12"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250608.0",
    "wrangler": "^3.107.0",
    "typescript": "^5.6.3"
  }
}
